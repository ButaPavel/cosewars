//function invert(array) {
    //arr.sort( (a, b) => a - b );
array = [1, 2 ,3]
 ar = array.map( (item) => (-item) )


    /*  var arrLength = array.map( function(item) {
    return -item;
  });*/
   console.log(ar) ;
//}